<UserControl x:Class="UI.Controls.ExternalLinkRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="400"
             x:Name="Root">

    <StackPanel Orientation="Horizontal" Margin="{Binding RowMargin, ElementName=Root}">
        <Image Width="25" Height="25"
               Margin="0,0,12,0"
               Source="{Binding IconSource, ElementName=Root, Converter={StaticResource StringToImageSourceConverter}}"/>
        <TextBlock FontSize="16" VerticalAlignment="Center">
            <Hyperlink Style="{StaticResource NeonHyperlink}"
                       NavigateUri="{Binding NavigateUri, ElementName=Root}"
                       RequestNavigate="OnRequestNavigate">
                <Run Text="{Binding LinkText, ElementName=Root, Mode=OneWay}"/>
            </Hyperlink>
        </TextBlock>
    </StackPanel>
</UserControl>