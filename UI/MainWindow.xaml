<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Stream Drop Collector"
        MinHeight="700" Height="700"
        MinWidth="1100" Width="1100"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        Background="{DynamicResource MainBackgroundBrush}"
        WindowStartupLocation="CenterScreen">

    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="1" ResizeBorderThickness="5" CaptionHeight="0"/>
    </WindowChrome.WindowChrome>

    <!-- YOUR STYLES — ONLY COLORS ARE NOW THEME-AWARE -->
    <Window.Resources>
        <!-- Sidebar Nav Button — YOUR EXACT #3A3A3A HOVER PRESERVED -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SidebarHoverBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Titlebar Min/Max Button — Clean hover, theme-aware -->
        <Style x:Key="TitleBarMinMaxButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Width" Value="{Binding ActualHeight, RelativeSource={RelativeSource Self}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource TitleBarButtonHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close Button — Red hover, always -->
        <Style x:Key="CloseBtn" TargetType="Button" BasedOn="{StaticResource TitleBarMinMaxButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border x:Name="RootBorder" Padding="0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="7*" MaxHeight="60"/>
                <RowDefinition Height="93*"/>
            </Grid.RowDefinitions>

            <!-- SIDEBAR — YOUR SOUL, NOW THEMED -->
            <Border Background="{DynamicResource CardBackgroundBrush}" Grid.Row="1">
                <StackPanel Margin="0,20,0,0">
                    <TextBlock Text="StreamDropCollector"
                               Foreground="{DynamicResource AccentBrush}"
                               FontWeight="Bold"
                               FontSize="16"
                               HorizontalAlignment="Center"
                               Margin="0 0 0 25"/>
                    <Button Content="Dashboard" Tag="Dashboard" Click="SidebarButton_Click" Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Inventory" Tag="Inventory" Click="SidebarButton_Click" Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Settings" Tag="Settings" Click="SidebarButton_Click" Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Help" Tag="Help" Click="SidebarButton_Click" Style="{StaticResource NavButtonStyle}"/>
                </StackPanel>
            </Border>

            <!-- HEADER — YOUR EXACT VIEWBOXES + TEXT -->
            <StackPanel x:Name="customApplicationHeader" 
                        Background="{DynamicResource HeaderBackgroundBrush}" 
                        MouseLeftButtonDown="OnWindowHeaderMouseDown" 
                        Grid.Row="0" Grid.ColumnSpan="2" 
                        Orientation="Horizontal">
                <Image Margin="10,10,10,10" Height="Auto" MaxHeight="75" Source="/Assets/logo.png" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Viewbox Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8" MaxHeight="40">
                    <StackPanel Orientation="Vertical">
                        <TextBlock x:Name="MainAppHeader" Text="Stream Drop Collector" FontFamily="Roboto" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="by tsgsOFFICIAL" FontStyle="Italic" FontFamily="Roboto" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Viewbox>
                <Viewbox Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8" MaxHeight="40">
                    <TextBlock x:Name="MainAppVersion" FontStyle="Italic" FontFamily="Roboto" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                </Viewbox>
            </StackPanel>

            <!-- YOUR ICONS — PERFECT HOVER, NO HARD CODED COLORS -->
            <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Name="Min_Button"       Content="&#x1F5D5;"   Click="OnMinimizeButtonClicked"   Style="{StaticResource TitleBarMinMaxButton}" ToolTip="Minimize"/>
                <Button x:Name="Maximize_Button" Content="&#x1F5D6;" Click="OnMaximizeButtonClicked" Style="{StaticResource TitleBarMinMaxButton}" ToolTip="Maximize"/>
                <Button Name="Close_Button"     Content="&#10006;"      Click="OnCloseButtonClicked"      Style="{StaticResource CloseBtn}"              ToolTip="Close"/>
            </StackPanel>

            <!-- MAIN CONTENT — EXACTLY AS YOU WROTE IT -->
            <Border Grid.Column="1" Grid.Row="1" Background="{DynamicResource MainBackgroundBrush}">
                <ContentControl x:Name="MainContent"/>
            </Border>

            <!-- NotifyIcon, that minimizes to system tray -->
            <tb:TaskbarIcon x:Name="MyNotifyIcon" IconSource="/Assets/logo.png" ToolTipText="Stream Drop Collector by tsgsOFFICIAL" DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Visibility="Collapsed">
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <!--MenuItem with centered icon and text-->
                        <MenuItem Header="Stream Drop Collector" IsEnabled="False" Foreground="LightGray">
                            <MenuItem.Icon>
                                <Canvas Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Image Source="/Assets/logo.png" Width="24" Height="24" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6, 0"/>
                                </Canvas>
                            </MenuItem.Icon>
                        </MenuItem>

                        <Separator Margin="0"/>

                        <MenuItem x:Name="JoinDiscord" Header="Join Discord" Command="{Binding JoinDiscordCommand}">
                            <MenuItem.Icon>
                                <Canvas Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Image Source="/Assets/discord.png" Width="24" Height="24" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6, 0"/>
                                </Canvas>
                            </MenuItem.Icon>
                        </MenuItem>

                        <MenuItem x:Name="OpenGithub" Header="Open Github" Command="{Binding OpenGithubCommand}">
                            <MenuItem.Icon>
                                <Canvas Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Image Source="/Assets/github.png" Width="24" Height="24" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6, 0"/>
                                </Canvas>
                            </MenuItem.Icon>
                        </MenuItem>

                        <MenuItem x:Name="MinimizeAndRestore" Header="Minimize" Command="{Binding ToggleWindowCommand}" />
                        <MenuItem Header="Exit" Command="{Binding CloseCommand}" />

                        <!--Define a style for the ContextMenu-->
                        <ContextMenu.Resources>
                            <!--Set background and foreground colors for MenuItems-->
                            <Style TargetType="MenuItem">
                                <Setter Property="Background" Value="#353535"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontFamily" Value="Roboto"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Padding" Value="10, 6"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Margin" Value="0,1,0,1"/>

                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type MenuItem}">
                                            <Border x:Name="templateRoot" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                                                <Grid VerticalAlignment="Center">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ContentPresenter x:Name="Icon" ContentSource="Icon" HorizontalAlignment="Center" Height="16" Width="16" Margin="2,5,5,5" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                                                    <ContentPresenter ContentSource="Header" Grid.Column="1" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="Icon" Value="{x:Null}">
                                                    <Setter Property="Visibility" TargetName="Icon" Value="Collapsed"/>
                                                </Trigger>
                                                <Trigger Property="IsHighlighted" Value="True">
                                                    <Setter Property="Background" TargetName="templateRoot" Value="#999999"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="TextElement.Foreground" TargetName="templateRoot" Value="LightGray"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>

                            <!--Style for the ContextMenu itself-->
                            <Style TargetType="ContextMenu">
                                <Setter Property="Background" Value="#353535"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ContextMenu">
                                            <Border 
                                                Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="6" 
                                                SnapsToDevicePixels="True" 
                                                Padding="10">

                                                <Grid>
                                                    <ItemsPresenter/>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>

                            <!--Style for separators-->
                            <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
                                <Setter Property="OverridesDefaultStyle" Value="true" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type Separator}">
                                            <Border Height="1" Margin="4,4,4,0" Background="LightGray" HorizontalAlignment="Stretch"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ContextMenu.Resources>
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </Grid>
    </Border>
</Window>